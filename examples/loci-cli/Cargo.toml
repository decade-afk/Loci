[package]
name = "loci-cli"
version = "0.1.0"
edition = "2021"
authors = ["Creative Studio Team"]
description = "Phase 1 verification tool for Loci AI inference engine"

[[bin]]
name = "loci-cli"
path = "src/main.rs"

[dependencies]
loci = { path = "../.." }
anyhow = "1.0"
env_logger = "0.11"
