[package]
name = "token-banner-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
loci-wasm-plugin-sdk = { path = "../../../wasm-plugin-sdk" }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
