[package]
name = "sdk-local"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[features]
default = ["candle", "gguf", "kernels-llama"]
candle = ["loci-sdk/candle"]
gguf = ["loci-sdk/gguf"]
kernels-llama = ["loci-sdk/kernels-llama"]
openvino = ["loci-sdk/openvino"]

[dependencies]
loci-sdk = { path = "../../crates/sdk" }
anyhow.workspace = true
