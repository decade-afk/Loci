apiVersion: v1
kind: Service
metadata:
  name: {{ include "loci.fullname" . }}
  labels:
    {{- include "loci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpc.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.service.rest.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loci.selectorLabels" . | nindent 4 }}
