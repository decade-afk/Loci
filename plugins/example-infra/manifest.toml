name = "example-infra"
version = "0.1.0"
api_version = "1.0"
kind = "hardware_backend"
description = "Example hardware backend plugin manifest for local acceleration policy."
auto_activate = true
priority = 90

[capabilities]
hardware_targets = ["cpu", "cuda", "vulkan"]
features = ["auto_detect", "mixed_offload", "fallback_cpu"]
