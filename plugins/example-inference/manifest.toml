name = "example-inference"
version = "0.1.0"
api_version = "1.0"
kind = "model_loader"
description = "Example local model loader plugin for the Loci infra track."
auto_activate = true
priority = 100

[capabilities]
model_formats = ["gguf"]
features = ["dynamic_load", "local_models"]
